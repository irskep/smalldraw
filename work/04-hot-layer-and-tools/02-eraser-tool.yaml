id: eraser-tool
status: complete
summary: Add an eraser tool that emits pen geometry with destination-out compositeOp.
context:
  - The design treats eraser as a pen stroke with compositeOp destination-out.
  - This keeps the data model uniform and tile rendering straightforward.
prerequisites:
  - work/01-core-foundation/02-core-model-extensions.yaml
acceptance_criteria:
  - Eraser tool uses pen geometry and compositeOp destination-out.
  - Tool works with drafts and commits like the existing pen tool.
  - Tests verify compositeOp behavior and geometry output.
  - All tests/typecheck/lint pass:
    - bun --filter @smalldraw/core test
    - bun --filter @smalldraw/core ts:check
    - bun --filter @smalldraw/core lint
  - mise run validate:all
notes:
  - Do not remove selection or other tools.
