id: renderer-canvas-scaffold
status: complete
summary: Create packages/renderer-canvas with a minimal API and test harness.
context:
  - The design requires direct CanvasRenderingContext2D rendering without Konva.
  - This package should be reusable by a frameworkless tile renderer.
prerequisites:
  - docs/thinking/infinite_kid_pix/design.md
  - work/01-core-foundation/02-core-model-extensions.yaml
acceptance_criteria:
  - New package with build/test/ts:check scripts wired for Bun.
  - Snapshot test harness established (node-canvas + looks-same).
  - Exports a stable entry point for render helpers.
  - All tests/typecheck/lint pass:
    - bun --filter @smalldraw/renderer-canvas test
    - bun --filter @smalldraw/renderer-canvas ts:check
    - bun --filter @smalldraw/renderer-canvas lint
  - mise run validate:all
notes:
  - Rendering accepts a pre-existing canvas context.
  - Keep APIs minimal and UI-agnostic.
