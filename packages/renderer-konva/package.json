{
  "name": "@smalldraw/renderer-konva",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "ts:check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint script yet'",
    "test": "bun test --preload ./src/__tests__/setup.ts",
    "test:update": "UPDATE_SNAPSHOTS=1 bun test --preload ./src/__tests__/setup.ts"
  },
  "dependencies": {
    "@smalldraw/core": "workspace:*",
    "konva": "^9.3.14",
    "perfect-freehand": "^1.2.1"
  },
  "devDependencies": {
    "canvas": "^3.1.0",
    "looks-same": "^7.0.0"
  }
}
