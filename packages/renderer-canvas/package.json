{
  "name": "@smalldraw/renderer-canvas",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "ts:check": "tsc -p tsconfig.json --noEmit",
    "lint": "bunx biome check --write",
    "test": "bun test"
  },
  "dependencies": {
    "@smalldraw/core": "workspace:*"
  },
  "devDependencies": {
    "canvas": "^3.1.0"
  }
}
