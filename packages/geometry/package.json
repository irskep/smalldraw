{
  "name": "@smalldraw/geometry",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "ts:check": "tsc -p tsconfig.json --noEmit",
    "lint": "bunx biome lint --write",
    "test": "bun test"
  },
  "dependencies": {
    "@mathigon/euclid": "^1.2.4",
    "gl-matrix": "^4.0.0-beta.2"
  }
}
